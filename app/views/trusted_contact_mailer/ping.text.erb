确认 <%= @user.display_name_or_email %> 是否平安

你被列为 <%= @app_name %> 上 <%= @user.sender_label %> 的可信联系人。
对方错过了签到，正在请求你确认其平安但暂时无法联系。

在此确认：
<%= @confirm_url %>

这将延后自动发送一段时间。你无法阅读任何消息或访问账户。
如果无法确认，可忽略此邮件。
