请在 <%= @app_name %> 上确认签到
<%= "=" * 40 %>

请确认你一切正常。

提醒第 <%= @attempt_number %> 次 / 共 <%= @attempt_total %> 次。

<% if @next_checkin_at %>
签到截止时间：<%= @next_checkin_at.strftime("%Y-%m-%d %H:%M UTC") %>
<% end %>

在此确认签到：
<%= @checkin_url %>

如果仍未确认，我们会每 <%= ((@user.effective_checkin_attempt_interval_hours) / 24.0).round %> 天提醒一次。
最终提醒后仍未确认，消息将自动发送给收件人。

<% if @next_attempt_at && @attempt_number < @attempt_total %>
下一次提醒：<%= @next_attempt_at.strftime("%Y-%m-%d %H:%M UTC") %>
<% end %>

---

此邮件由 <%= @app_name %> 自动发送。
