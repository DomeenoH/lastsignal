紧急：最终提醒 - 你的 <%= @app_name %> 消息即将发送
<%= "=" * 60 %>

这是你在 <%= @app_name %> 上的最终提醒。除非你采取行动，否则你的消息将自动发送给收件人。

提醒第 <%= @attempt_number %> 次 / 共 <%= @attempt_total %> 次。

*** 预计发送时间：<%= @delivery_due_at&.strftime("%Y-%m-%d %H:%M UTC") || "即将" %> ***

如果你仍然平安并希望停止发送，请立即确认签到：
<%= @checkin_url %>

点击此链接将：
- 取消计划中的自动发送
- 将你恢复为正常状态
- 安排下一次签到

如果你不点击链接，消息将自动发送给收件人。

---

此邮件由 <%= @app_name %> 自动发送。
