你的消息已发送
<%= "=" * 40 %>

你的 失联告警 · LastSignal 消息已发送给收件人。

发送时间：<%= @delivered_at&.strftime("%Y-%m-%d %H:%M UTC") || "刚刚" %>

<% if @recipient_emails.any? %>
已通知收件人：
<% @recipient_emails.each do |email| %>
- <%= email %>
<% end %>
<% end %>

---

此邮件由 <%= @app_name %> 自动发送。
