<% content_for(:title) { "登录 - LastSignal" } %>

<div class="max-w-md mx-auto mt-16">
  <div class="text-center mb-8">
    <div class="flex items-center justify-center gap-3 mb-2">
      <%= image_tag "/logo-mark.svg", width: 48, height: 48, class: "flex-shrink-0", alt: "LastSignal 标志" %>
      <h1 class="text-3xl font-bold text-gray-900">LastSignal</h1>
    </div>
    <p class="mt-2 text-gray-600">当你无法按时签到时，消息会自动发送给你关心的人</p>
  </div>

  <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-8">
    <h2 class="text-xl font-semibold text-gray-900 mb-6">登录</h2>

    <%= form_with url: magic_link_path, method: :post, class: "space-y-6" do |f| %>
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700">
          邮箱地址
        </label>
        <input 
          type="email" 
          name="email" 
          id="email" 
          required
          autofocus
          autocomplete="email"
          class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
          placeholder="you@example.com"
        >
      </div>

      <div>
        <button type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
          发送登录链接
        </button>
      </div>
    <% end %>

    <p class="mt-6 text-center text-sm text-gray-500">
      无需密码，我们会发送一次性登录链接到你的邮箱。
    </p>

  </div>

  <div class="mt-6 text-center text-sm text-gray-500">
    邮箱无法访问？使用<%= link_to "紧急停止", emergency_path, class: "font-medium text-red-600 underline hover:text-red-700" %>。
  </div>

  <div class="mt-8 p-4 bg-blue-50 border border-blue-200 rounded-lg">
    <% if AppConfig.allowlist_enabled? %>
      <p class="text-sm text-blue-700">
        私有实例：仅白名单邮箱可登录。
      </p>
    <% else %>
      <h3 class="text-sm font-medium text-blue-800">第一次使用？</h3>
      <p class="mt-1 text-sm text-blue-700">
        输入邮箱即可继续；没有账号会自动为你创建。
      </p>
    <% end %>
  </div>
</div>
