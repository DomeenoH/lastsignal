来自 <%= @app_name %> 的好消息
<%= "=" * 40 %>

<%= @recipient.display_name %> 已接受邀请并完成安全设置。

现在你可以将他们作为消息的收件人。

接受时间：<%= @accepted_at&.strftime("%Y-%m-%d %H:%M UTC") %>

登录以创建或更新消息：
<%= login_url %>

---

此邮件由 <%= @app_name %> 自动发送。
